# Wakaama Docker 설치 방법

이 문서는 다른 서버에 Wakaama Docker runtime을 설치하는 방법을 설명한다.
설치 방법은 두 가지다.

- **방법 A: Git clone 후 target 서버에서 Docker image build**
- **방법 B: Docker image를 export/import하여 source 없이 설치**

두 방법 모두 Docker container 하나에서 다음을 실행한다.

```text
Dashboard
Bootstrap Server
LwM2M Server
```

container가 시작되면 Dashboard만 먼저 실행된다. Bootstrap Server와 LwM2M Server는 Dashboard의 Start 버튼으로 시작한다.

`01_bs_plain.ini`는 별도 변환하지 않는다. 현재 사용하는 파일을 그대로 다음 위치에 복사해서 사용한다.

```text
docker-data/config/01_bs_plain.ini
```

---

## 1. 공통 사전 준비

### 1.1 Target 서버 요구사항

Target 서버에 다음이 설치되어 있어야 한다.

- Linux
- Docker Engine
- Docker Compose plugin
- `git` 사용 가능 여부는 설치 방법에 따라 다름
- 외부 장비가 접속할 경우 UDP port를 허용할 수 있는 firewall 권한

Docker와 Compose 설치 여부를 확인한다.

```bash
docker --version
docker compose version
```

### 1.2 Network port

기본 port는 다음과 같다.

| 용도 | Protocol | 기본 port |
|---|---:|---:|
| Dashboard | TCP | 8080 |
| Bootstrap Server | UDP | 22101 |
| LwM2M Server | UDP | 22102 |

외부 device가 접속하는 운영 서버라면 target 서버의 firewall과 외부 NAT/firewall에서 UDP `22101`, `22102`를 허용해야 한다.

`PUBLIC_ENDPOINT`와 `01_bs_plain.ini`의 LwM2M server URI는 device가 실제로 접근할 수 있는 public IP 또는 FQDN이어야 한다.

### 1.3 공통 운영 파일

두 설치 방법 모두 다음 파일을 준비한다.

```text
.env
  Docker port, public endpoint, DFOTA host, PUID/PGID 설정

docker-data/
├── config/
│   └── 01_bs_plain.ini
├── firmware/
├── backups/
└── logs/
```

`01_bs_plain.ini`는 현재 파일을 그대로 사용한다.

```bash
mkdir -p docker-data/config docker-data/firmware docker-data/backups docker-data/logs
cp /path/to/01_bs_plain.ini docker-data/config/01_bs_plain.ini
```

다음 runtime socket은 복사하지 않는다.

```text
docker-data/run/lwm2mserver-control.sock
```

이 파일은 container 실행 중 새로 생성되는 Unix socket이다. 이전 서버의 socket 파일이 있다면 target 서버에서 삭제한다.

```bash
rm -f docker-data/run/lwm2mserver-control.sock
```

### 1.4 `.env` 준비

`.env.example`을 복사한 뒤 실제 운영값으로 수정한다.

```bash
cp .env.example .env
vi .env
```

예시:

```dotenv
PUBLIC_ENDPOINT=115.90.109.11
BOOTSTRAP_HOST_PORT=22101
LWM2M_HOST_PORT=22102
DASHBOARD_PORT=8080
DFOTA_HOST=115.90.109.11
DFOTA_URI_PREFIX=/dfota_fw
WAKAAMA_COMMERCIAL_SERVER_MODE=1
PUID=1001
PGID=1001
```

`PUBLIC_ENDPOINT`는 예시값이 아니라 실제 device가 접근 가능한 주소로 설정해야 한다.

`PUID`, `PGID`는 target 서버에서 다음 명령으로 확인할 수 있다.

```bash
id -u
id -g
```

다른 사용자로 운영하면 `.env`의 `PUID`, `PGID`를 target 사용자 값으로 변경한다.

`.env`와 `01_bs_plain.ini`에는 운영 주소와 credential이 포함될 수 있으므로 공개 repository에 commit하지 않는다.

### 1.5 LAN 접근 제한

Docker Compose는 Dashboard와 LwM2M/Bootstrap UDP port를 host에 publish한다. 같은 사설망의 다른 PC에서 접근해야 하면 compose port publish는 `0.0.0.0`으로 열고, 접속 출발지 제한은 host firewall에서 처리한다.

이 project에서 사용하는 기본 port는 다음과 같다.

```text
Dashboard HTTP: 8080/tcp
Bootstrap Server: 22101/udp
LwM2M Server: 22102/udp
```

예를 들어 `172.28.200.0/24` 대역에서만 접근을 허용하려면 Docker가 publish한 port에 대해 `DOCKER-USER` chain에서 다른 출발지를 차단한다.

```bash
sudo iptables -I DOCKER-USER 1 -p tcp --dport 8080 ! -s 172.28.200.0/24 -j DROP
sudo iptables -I DOCKER-USER 2 -p udp -m multiport --dports 22101,22102 ! -s 172.28.200.0/24 -j DROP
```

위 규칙은 Docker port publish가 `0.0.0.0`으로 되어 있어도 출발지 IP가 `172.28.200.0/24`가 아니면 접근을 차단한다. 방화벽 규칙은 재부팅 후 사라질 수 있으므로 운영 OS의 방식에 맞게 영구 저장한다.

---

# 방법 A. Git clone 후 Target 서버에서 Build

이 방법은 target 서버가 Git repository에 접근할 수 있고, Docker image를 target 서버에서 직접 만들 수 있을 때 사용한다.

## A-1. Source 받기

```bash
mkdir -p ~/Wakaama
cd ~/Wakaama
git clone <repository-url> 05_Wakaama
cd 05_Wakaama
```

이미 checkout이 있으면 최신 source를 받는다.

```bash
cd ~/Wakaama/05_Wakaama
git pull
```

## A-2. `.env` 복사

기존 운영 서버의 `.env`를 그대로 사용하려면 안전한 방법으로 복사한다.

기존 서버에서 실행한다.

```bash
scp ~/Wakaama/05_Wakaama/.env \
    user@target-server:~/Wakaama/05_Wakaama/.env
```

또는 target 서버에서 `.env.example`을 복사한 뒤 직접 수정한다.

```bash
cd ~/Wakaama/05_Wakaama
cp .env.example .env
vi .env
```

## A-3. `01_bs_plain.ini` 그대로 복사

기존 운영 서버의 현재 파일을 target 서버의 persistent config 위치로 복사한다.

```bash
ssh user@target-server \
  'mkdir -p ~/Wakaama/05_Wakaama/docker-data/config'

scp ~/Wakaama/05_Wakaama/docker-data/config/01_bs_plain.ini \
    user@target-server:~/Wakaama/05_Wakaama/docker-data/config/01_bs_plain.ini
```

현재 파일이 repository의 `run/bootstrap_server/01_bs_plain.ini`라면 다음처럼 복사한다.

```bash
scp run/bootstrap_server/01_bs_plain.ini \
    user@target-server:~/Wakaama/05_Wakaama/docker-data/config/01_bs_plain.ini
```

Docker entrypoint는 다음 규칙으로 동작한다.

```text
/data/config/01_bs_plain.ini가 있으면
    기존 파일을 그대로 사용

/data/config/01_bs_plain.ini가 없으면
    image에 포함된 기본 INI를 복사
```

따라서 target에서 Docker를 시작하기 전에 파일을 배치하면 현재 endpoint, lifetime, URI, credential을 그대로 사용할 수 있다.

## A-4. DFOTA file 복사

기존 firmware도 사용해야 한다면 복사한다.

```bash
rsync -av \
  docker-data/firmware/ \
  user@target-server:~/Wakaama/05_Wakaama/docker-data/firmware/

rsync -av docker-data/firmware/ quectel@localhost:~/Wakaama/test/docker-data/firmware/
```

필요하면 backup과 과거 log도 복사할 수 있다.

```bash
rsync -av docker-data/backups/ \
  user@target-server:~/Wakaama/05_Wakaama/docker-data/backups/

rsync -av docker-data/logs/ \
  user@target-server:~/Wakaama/05_Wakaama/docker-data/logs/
```

## A-5. 설정 확인

Target 서버에서 확인한다.

```bash
cd ~/Wakaama/05_Wakaama
sha256sum .env docker-data/config/01_bs_plain.ini
```

기존 서버에서 같은 명령을 실행한 결과와 hash가 같은지 확인한다.

## A-6. Docker 시작

```bash
cd ~/Wakaama/05_Wakaama
./start_server.sh
```

`start_server.sh`는 내부적으로 다음 작업을 한다.

```text
1. Docker/Compose 확인
2. .env 확인
3. Dockerfile의 builder stage에서 binary compile
4. runtime image 생성
5. container 시작
6. docker compose ps 출력
```

image build만 직접 수행하려면:

```bash
docker compose build
```

## A-7. Dashboard에서 server 시작

브라우저에서 `.env`의 설정에 따라 접속한다.

```text
http://<target-server-ip>:8080
```

예를 들어 target 서버의 LAN 주소가 `172.28.200.50`이면 `http://172.28.200.50:8080`으로 접속한다.

Dashboard에서 다음 순서로 실행한다.

1. Bootstrap Server의 `Start` 클릭
2. LwM2M Server의 `Start` 클릭
3. device를 재등록 또는 재-bootstrap
4. Registered Clients와 log 확인

상태 확인:

```bash
./show_api_status.sh
```

Docker log 확인:

```bash
docker compose logs -f wakaama
```

container 내부 확인:

```bash
docker compose exec wakaama sh
```

## A-8. 종료와 재시작

종료:

```bash
./stop_docker.sh
```

재시작:

```bash
./start_server.sh
```

설정, endpoint, firmware, backup, log는 `docker-data/`에 있으므로 container를 종료해도 유지된다.

---

# 방법 B. Docker Image Export/Import로 Source 없이 설치

이 방법은 target 서버에서 Git clone이나 C/C++ compile을 하지 않고, 이미 검증한 Docker image를 그대로 배포할 때 사용한다.

## B-1. Source 서버에서 image build

Source 서버에서 project root로 이동한다.

```bash
cd ~/Wakaama/05_Wakaama
docker compose build
```

image를 확인한다.

```bash
docker image ls wakaama-dashboard:local
```

필요하면 image ID를 확인한다.

```bash
docker image inspect wakaama-dashboard:local \
  --format '{{.Id}} {{.Architecture}}/{{.Os}}'
```

## B-2. Image export

```bash
docker save wakaama-dashboard:local | gzip > wakaama-dashboard-local.tar.gz
```

생성된 파일을 확인한다.

```bash
ls -lh wakaama-dashboard-local.tar.gz
```

## B-3. 운영 파일 전달

Image와 함께 다음 파일을 target 서버로 복사한다.
`docker compose up`은 image만으로는 실행할 수 없고, 실행 directory 안에 `compose.yaml`과 `.env`, `docker-data/`가 있어야 한다.

```text
.env
docker-data/config/01_bs_plain.ini
docker-data/firmware/       선택
docker-data/backups/        선택
docker-data/logs/           선택
compose.yaml
start_docker.sh
stop_docker.sh
show_api_status.sh
```

예시:

```bash
ssh user@target-server 'mkdir -p ~/wakaama-deploy'

scp wakaama-dashboard-local.tar.gz  user@target-server:~/wakaama-deploy/

scp .env compose.yaml start_docker.sh stop_docker.sh show_api_status.sh  user@target-server:~/wakaama-deploy/

rsync -av docker-data/  user@target-server:~/wakaama-deploy/docker-data/
```

`docker-data/run/`은 복사하지 않는다.

동일 시스템에서 image-only 배포 절차를 시험하려면 별도 directory를 만들고 같은 파일을 복사한다.

```bash
mkdir -p ~/Wakaama/docker_image
cp wakaama-dashboard-local.tar.gz .env compose.yaml start_docker.sh stop_docker.sh show_api_status.sh  ~/Wakaama/docker_image/
rsync -av docker-data/ ~/Wakaama/docker_image/docker-data/
```

## B-4. Target 서버에서 image import

```bash
cd ~/wakaama-deploy
docker load < wakaama-dashboard-local.tar.gz
```

image가 로드됐는지 확인한다.

```bash
docker image ls wakaama-dashboard:local
```

CPU architecture도 확인한다.

```bash
uname -m
docker image inspect wakaama-dashboard:local \
  --format '{{.Architecture}}/{{.Os}}'
```

Source 서버와 Target 서버의 architecture가 다르면 image를 그대로 실행할 수 없을 수 있다.

## B-5. Source 없이 Compose로 시작

Source가 없는 target에서는 `docker compose build`를 실행하지 않는다.
반드시 `compose.yaml`이 있는 배포 directory에서 실행한다. 빈 directory나 image tar file만 있는 directory에서 실행하면 `no configuration file provided: not found` 오류가 발생한다.

다음 명령을 사용한다.

```bash
cd ~/wakaama-deploy
./start_docker.sh
```

`start_docker.sh`는 내부에서 다음 명령을 실행한다.

```bash
docker compose up -d --no-build
docker compose ps
```

image tag `wakaama-dashboard:local`이 실제로 로드된 tag와 같아야 한다.

## B-6. Dashboard에서 server 시작

```text
http://127.0.0.1:8080
```

Dashboard에서 Bootstrap Server와 LwM2M Server의 Start 버튼을 누른다.

확인:

```bash
./show_api_status.sh
docker compose logs -f wakaama
```

## B-7. Source 제거

Image import와 운영 data 배치가 완료되고 container가 정상적으로 기동되는 것을 확인한 뒤 source directory를 삭제할 수 있다.

```bash
rm -rf ~/Wakaama/05_Wakaama
```

단, source를 삭제하면 다음 작업은 할 수 없다.

```bash
docker compose build
./start_server.sh
```

`start_server.sh`는 내부에서 `docker compose up -d --build`를 실행하기 때문이다. Source를 삭제한 뒤에는 반드시 `start_docker.sh` 또는 다음 명령을 사용한다.

```bash
docker compose up -d --no-build
```

또한 source 삭제 전에 `compose.yaml`, `.env`, `docker-data/`, `start_docker.sh`, `stop_docker.sh`를 별도 배포 directory에 보관해야 한다.

---

## 4. 두 방법의 차이

| 항목 | 방법 A: Git clone/build | 방법 B: Image export/import |
|---|---|---|
| Target에 Git 필요 | 필요 | 불필요 |
| Target에서 compiler 필요 | Docker builder를 위해 필요 | 불필요 |
| Target에서 image build | 수행 | 수행하지 않음 |
| Target architecture에 맞춘 build | 가능 | source image architecture에 의존 |
| 배포 속도 | build 시간 필요 | image load 후 바로 실행 |
| source 수정 후 재빌드 | 가능 | source 없으면 불가 |
| 동일 image 보장 | target에서 다시 build | export한 image 그대로 사용 |
| 추천 용도 | 개발/일반 운영 | 폐쇄망/검증 image 고정 운영 |

## 5. 보안 및 운영 주의사항

### Credential 보호

`01_bs_plain.ini`에는 server URI뿐 아니라 public/secret 또는 credential이 포함될 수 있다.
`.env`에도 public endpoint 정보가 들어간다. 전송 시 `scp`, `rsync over SSH`, private storage를 사용한다.
공개 repository나 공개 Docker registry에는 업로드하지 않는다.

### Port 충돌

Docker를 시작하기 전에 같은 host에서 local console, tmux, systemd 방식의 Bootstrap Server와 LwM2M Server를 실행하지 않는다.
같은 UDP port를 동시에 사용할 수 없다.

```bash
ss -ltnup | grep -E ':(8080|22101|22102)\\b' || true
```

### Persistent data 삭제 금지

다음 명령은 Docker volume을 삭제할 수 있으므로 운영 환경에서 사용하지 않는다.

```bash
docker compose down -v
```

일반 종료는 다음을 사용한다.

```bash
./stop_docker.sh
```

`docker-data/`는 별도로 backup한다.

### Control socket

다음 파일은 runtime socket이므로 서버 간에 복사하지 않는다.

```text
docker-data/run/lwm2mserver-control.sock
```

container가 시작될 때 새로 만들어진다.

## 6. 문제 해결

### `PUBLIC_ENDPOINT` 오류

`.env`가 없거나 예시값인 경우다.

```bash
cp .env.example .env
vi .env
```

실제 public IP 또는 FQDN을 넣은 뒤 다시 시작한다.

### `image not found` 오류

방법 B라면 image가 import되지 않았거나 tag가 다를 수 있다.

```bash
docker load < wakaama-dashboard-local.tar.gz
docker image ls wakaama-dashboard:local
```

### `port is already allocated` 오류

기존 server/dashboard가 port를 사용 중인지 확인한다.

```bash
ss -ltnup | grep -E ':(8080|22101|22102)\\b' || true
```

기존 local service를 종료하거나 `.env`의 host port를 변경한다.

### Bootstrap 설정이 반영되지 않음

Bootstrap Server는 시작할 때 INI를 읽는다.

```text
01_bs_plain.ini 변경
    ↓
Dashboard의 Restart Bootstrap
    ↓
device 재-bootstrap
```

이미 등록된 device에 변경된 lifetime이 자동 적용되는 것은 아니다.

### Dashboard에 client가 보이지 않음

다음 순서로 확인한다.

```bash
./show_api_status.sh
docker compose logs -f wakaama
cat docker-data/config/01_bs_plain.ini
```

Bootstrap Server와 LwM2M Server가 모두 Dashboard에서 Start되어 있고, device가 `PUBLIC_ENDPOINT`와 UDP port에 접근 가능한지 확인한다.

---

## 7. 설치 완료 확인

```bash
cd ~/Wakaama/05_Wakaama
./show_api_status.sh
```

정상적으로 server를 시작한 뒤 다음 항목이 표시되어야 한다.

```json
{
  "bootstrap": {
    "listener": true,
    "running": true,
    "manager": "dashboard"
  },
  "server": {
    "listener": true,
    "running": true,
    "manager": "dashboard"
  }
}
```

Dashboard 접속:

```text
http://127.0.0.1:8080
```

운영 중지:

```bash
./stop_docker.sh
```
