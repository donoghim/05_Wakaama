# 02_bs_server / 03_client / 05_server 통합 작업계획 (260911)

## 배경 조사 결과

- `01_org`, `02_bs_server`, `03_client`, `04_lightclient`, `05_server`, `06_shared`는 모두 동일한 Wakaama 소스(`core/coap/data/include`)를 폴더별로 통째로 복제한 것이다.
- 업스트림 Wakaama의 CMake 구조 자체가 하나의 소스 트리에서 `bootstrap_server`, `lwm2mclient`(+`_tinydtls`), `lwm2mserver`, `lightclient` 4개 실행 파일을 동시에 독립적으로 빌드하도록 설계되어 있다 (`examples/CMakeLists.txt` → `add_subdirectory`, 각 타겟은 `target_sources_wakaama()`로 동일 소스를 타겟별로 별도 컴파일). 따라서 하나로 합쳐도 빌드 단계에서 서로 간섭하지 않는다.
- 실제 폴더가 나뉜 이유는 각 폴더 루트에서 `cmake ./examples/<target>` 방식의 in-source 빌드를 했기 때문이다 (같은 폴더에서 다른 타겟을 cmake하면 이전 설정을 덮어씀). 소스 구조 자체의 문제는 아니다.
- 커스텀 수정 확인:
  - `02_bs_server`: `examples/bootstrap_server/{bootstrap_info.c, bootstrap_server.c, bootstrap_server.ini}` 수정
  - `03_client`: 소스 수정 없음 (원본과 동일)
  - `05_server`: `examples/server/lwm2mserver.c`에 DFOTA 로직 939줄 추가, `examples/shared/{connection.c,dtlsconnection.c}`와 `core/internals.h`, `include/liblwm2m.h`에 `#ifdef LWM2M_WITH_LOGS`→`#if 1` 강제 로그 on, `coap/er-coap-13/er-coap-13.{c,h}`에 `DEBUG 0→1`, `COAP_RESPONSE_TIMEOUT 2→5`, 포맷 스트링(`%u`→`%lu`) 수정
  - `04_lightclient`, `06_shared`: 원본과 동일, 미사용 사본

## 사용자 결정 사항

1. server 전용 튜닝(DEBUG / COAP_RESPONSE_TIMEOUT / 로그 강제 on)은 **server 타겟에만 스코프** (CMake `target_compile_definitions`로 전환, client/bootstrap_server는 기존 동작 유지)
2. `04_lightclient`는 통합 대상에서 **제외** (미사용 사본이라 신경 쓰지 않음, 단 원본 examples/ 구조상 자연히 포함되는 건 무방)
3. 실행 스크립트/설정 파일은 **역할별 `run/` 하위폴더**로 배치 (`run/bootstrap_server/`, `run/server/`, `run/client/`)
4. 통합 완료 후 기존 `01_org`, `02_bs_server`, `03_client`, `04_lightclient`, `05_server`, `06_shared`는 **삭제** (git 히스토리에 남아 있어 필요 시 복구 가능)

## 목표 디렉터리 구조

```
05_Wakaama/
├── wakaama/                          # 통합 소스 트리 (단일)
│   ├── CMakeLists.txt
│   ├── wakaama.cmake
│   ├── core/ coap/ data/ include/    # 공용 라이브러리 소스
│   ├── examples/
│   │   ├── CMakeLists.txt
│   │   ├── bootstrap_server/         # 02_bs_server 수정사항 반영
│   │   ├── client/                   # 03_client, 원본과 동일
│   │   ├── lightclient/              # 원본 그대로 (신경 안 씀)
│   │   ├── server/                   # 05_server의 lwm2mserver.c(DFOTA) 반영 + 전용 CMake define
│   │   └── shared/                   # connection.c/dtlsconnection.c 원본 형태로 복원
│   └── tests/
├── run/
│   ├── bootstrap_server/             # 02_bs_server의 *.sh, *.ini, psk.txt, key.txt
│   ├── server/                       # 05_server의 *.sh, dfota_fw/
│   └── client/                       # 실행 스크립트/노트 (필요 시)
├── build/                             # cmake -S wakaama -B build 산출물 (4개 실행파일 전부 생성)
└── 07_sender/                          # 기존 그대로 유지 (독립 유틸)
```

## 실행 단계

### 1단계: 소스 병합

- `01_org`를 베이스로 `wakaama/` 생성
- `02_bs_server/examples/bootstrap_server/{bootstrap_info.c, bootstrap_server.c, bootstrap_server.ini}` 이식
- `05_server/examples/server/lwm2mserver.c` 이식

### 2단계: server 전용 튜닝을 CMake 정의로 전환

- `core/internals.h`, `include/liblwm2m.h`: `#if 1 // LWM2M_WITH_LOGS` → `#ifdef LWM2M_WITH_LOGS` 원복
- `examples/shared/{connection.c,dtlsconnection.c}`: 동일하게 원복
- `coap/er-coap-13/er-coap-13.c`: `#define DEBUG 0` → `#ifndef DEBUG / #define DEBUG 0 / #endif`
- `coap/er-coap-13/er-coap-13.h`: `COAP_RESPONSE_TIMEOUT` 동일하게 `#ifndef` 가드 처리
- `examples/server/CMakeLists.txt`에 추가:
  ```cmake
  target_compile_definitions(lwm2mserver PRIVATE LWM2M_WITH_LOGS DEBUG=1 COAP_RESPONSE_TIMEOUT=5)
  ```
- `%u`→`%lu` 포맷 수정은 버그 수정이므로 공유 파일에 전역 유지
- `core/registration.c`의 공백 전용 diff는 원본(01_org) 포맷 채택

### 3단계: 실행 스크립트/설정 이전

- `02_bs_server`의 `*.sh`, `*.ini`, `psk.txt`, `key.txt` → `run/bootstrap_server/`
- `05_server`의 `*.sh`, `dfota_fw/` → `run/server/`
- 스크립트 내 바이너리 경로를 `../../build/examples/<target>/<binary>`로 수정
- 불필요한 산출물(`tags`, `log.txt`, `obs.txt`, `.bak` 파일, 빌드 바이너리 등)은 이관하지 않음

### 4단계: 빌드 검증

```sh
cmake -S wakaama -B build
cmake --build build -j
```

- `bootstrap_server`, `lwm2mclient`, `lwm2mclient_tinydtls`, `lwm2mserver` 4개 바이너리 생성 확인

### 5단계: 동작 검증

- `run/bootstrap_server/00_bs_plain.sh` → bootstrap 정상 동작 확인
- `run/server/11_server_sender_dfota.sh` → server 기동, client 등록/read/write/observe/DFOTA 절차 확인 (`LWM2M_SERVER_CLIENT_TEST_NOTE.md`, `readme_howtouse.md` 절차 기준)
- client/bootstrap_server 로그가 꺼져 있고 server만 로그/타임아웃 5초가 적용되는지 확인

### 6단계: 문서/정리

- `readme_howtouse.md`, `readme_howToCompile.txt`, `LWM2M_SERVER_CLIENT_TEST_NOTE.md`의 경로를 새 구조(`wakaama/`, `run/`, `build/`)에 맞게 갱신
- 검증 완료 후 `01_org`, `02_bs_server`, `03_client`, `04_lightclient`, `05_server`, `06_shared` 삭제
